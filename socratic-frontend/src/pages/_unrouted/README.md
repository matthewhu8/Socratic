# Unrouted pages

The app was refocused on the AI whiteboard tutor only (see the comment in `src/App.js`).
These pages predate that change and are not mounted on any route — kept on disk for
reference/possible reuse, not part of the live app. Matching dormant components live in
`src/components/_unrouted/`, and their styles in `src/styles/_unrouted/`.
